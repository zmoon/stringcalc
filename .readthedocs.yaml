version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.13"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc
        - cli

sphinx:
  configuration: docs/conf.py
